const priceMap={1:21900,2:39800};const mainImage=document.getElementById('mainImage');const thumbs=document.querySelectorAll('.thumb');const optionSelect=document.getElementById('optionSelect');const qtyInput=document.getElementById('qtyInput');const totalPrice=document.getElementById('totalPrice');const minusBtn=document.getElementById('minusBtn');const plusBtn=document.getElementById('plusBtn');function formatKRW(v){return new Intl.NumberFormat('ko-KR').format(v)+'원'}function updatePrice(){const option=optionSelect.value;const qty=Math.max(1,parseInt(qtyInput.value||'1',10));qtyInput.value=qty;totalPrice.textContent=formatKRW(priceMap[option]*qty)}thumbs.forEach(thumb=>{thumb.addEventListener('click',()=>{thumbs.forEach(btn=>btn.classList.remove('active'));thumb.classList.add('active');mainImage.src=thumb.dataset.image})});minusBtn.addEventListener('click',()=>{qtyInput.value=Math.max(1,parseInt(qtyInput.value||'1',10)-1);updatePrice()});plusBtn.addEventListener('click',()=>{qtyInput.value=Math.max(1,parseInt(qtyInput.value||'1',10)+1);updatePrice()});optionSelect.addEventListener('change',updatePrice);qtyInput.addEventListener('change',updatePrice);updatePrice();