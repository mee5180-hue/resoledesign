const mainImage=document.getElementById('mainImage');const thumbs=document.querySelectorAll('.thumb-strip .thumb');if(mainImage&&thumbs.length){thumbs.forEach(t=>t.addEventListener('click',()=>{thumbs.forEach(x=>x.classList.remove('active'));t.classList.add('active');mainImage.src=t.dataset.image}))}const optionSelect=document.getElementById('optionSelect');const qtyInput=document.getElementById('qtyInput');const totalPrice=document.getElementById('totalPrice');const minusBtn=document.getElementById('minusBtn');const plusBtn=document.getElementById('plusBtn');const priceMap={'1':21900,'2':39800};function updatePrice(){if(!optionSelect||!qtyInput||!totalPrice)return;const q=Math.max(1,parseInt(qtyInput.value||'1',10));qtyInput.value=q;totalPrice.textContent=new Intl.NumberFormat('ko-KR').format(priceMap[optionSelect.value]*q)+'원'}if(minusBtn)minusBtn.addEventListener('click',()=>{qtyInput.value=Math.max(1,+qtyInput.value-1);updatePrice()});if(plusBtn)plusBtn.addEventListener('click',()=>{qtyInput.value=Math.max(1,+qtyInput.value+1);updatePrice()});if(optionSelect)optionSelect.addEventListener('change',updatePrice);if(qtyInput)qtyInput.addEventListener('change',updatePrice);updatePrice();document.querySelectorAll('video').forEach(v=>{v.muted=true;v.playsInline=true;const p=v.play();if(p&&p.catch)p.catch(()=>{})});